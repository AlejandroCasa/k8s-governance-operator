apiVersion: v1
kind: Pod
metadata:
  name: fatal-attack
  namespace: team-beta
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      limits:
        cpu: "500m"
  restartPolicy: Never
